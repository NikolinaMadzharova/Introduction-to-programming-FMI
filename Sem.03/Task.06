#include <iostream>
using namespace std;
#include<cmath>
int main()
{
    int number;
    cin >> number;
    double sqrtOfNumber = sqrt(number);

    if (number <= 1) {
        cout << "Not prime";
    }
    else {
        bool isPrime = true;
        for (int i = 2; i <= sqrtOfNumber; i++) {
            if (number % i == 0) {
                isPrime = false;
                break;
            }
        }
        if (isPrime) {
            cout <<"Prime" << endl;
        }else{
            cout << "Not prime" << endl;
        }
       
    }
}
